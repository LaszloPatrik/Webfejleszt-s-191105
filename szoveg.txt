Gitre feldobás:

Hozz létre repositorit, utána a Terminalba:
1. git init
2. git add .
3. git remote add origin 'url'
    --> Ellenőrzés: git remote -v
    --> Törlés: git remote remove origin
4. git commit -n 'Megjegyzés'
5. git push -u origin master